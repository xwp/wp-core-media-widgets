<?xml version="1.0"?>
<ruleset name="WP Core Media Widget">

	<rule ref="WordPress-Core" />
	<rule ref="WordPress-Docs" />
	<rule ref="WordPress-Extra" />

	<rule ref="WordPress">
		<exclude name="WordPress.XSS.EscapeOutput"/>
	</rule>

	<!-- @todo These should not be required since the WordPress-VIP ruleset isn't being included. -->
	<rule ref="WordPress.WP.AlternativeFunctions.file_get_contents_file_get_contents">
		<severity>0</severity>
	</rule>
	<rule ref="WordPress.WP.AlternativeFunctions.file_system_read_file_get_contents">
		<severity>0</severity>
	</rule>
	<rule ref="WordPress.VIP.RestrictedFunctions.file_get_contents_file_get_contents">
		<severity>0</severity>
	</rule>
	<rule ref="WordPress.VIP.RestrictedFunctions.get_intermediate_image_sizes_get_intermediate_image_sizes">
		<severity>0</severity>
	</rule>

	<!--
	The following rule exclusions are due to a desire to do:
	'media_library_state' => _n_noop( 'Media Widget', 'Media Widget (%d)' ),
	Where PHPCS currently complains about the singular string missing a placeholder.
	For more, see https://github.com/xwp/wp-core-media-widgets/pull/27#discussion_r107043295
	-->
	<rule ref="WordPress.WP.I18n.MissingSingularPlaceholder">
		<exclude-pattern>wp-includes/widgets/class-wp-widget-*.php</exclude-pattern>
	</rule>
	<rule ref="WordPress.WP.I18n.MismatchedPlaceholders">
		<exclude-pattern>wp-includes/widgets/class-wp-widget-*.php</exclude-pattern>
	</rule>


	<exclude-pattern>*/dev-lib/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
</ruleset>
